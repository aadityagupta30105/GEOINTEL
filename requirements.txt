# Runtime dependencies.
#
# This is the deployment manifest: it installs what the pipeline and the
# dashboard need to run. The machine-learning stack is deliberately excluded
# because the dashboard never loads it and hosted environments cap install
# size; event classification degrades to the keyword classifier without it.
#
#   Event classification : pip install -r requirements-ml.txt
#   Development          : pip install -r requirements-dev.txt

# --- Core -------------------------------------------------------------------
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0

# --- Graph analysis ---------------------------------------------------------
# scipy is a hard requirement, not an optional accelerator: networkx implements
# pagerank and the dense eigenvector solver on scipy sparse matrices. It is
# declared explicitly rather than inherited from scikit-learn, which is not
# installed in the runtime manifest.
networkx>=3.1
scipy>=1.10.0

# --- Dashboard and visualisation -------------------------------------------
# Table shading uses the platform palette directly, so matplotlib is
# deliberately not a dependency.
streamlit>=1.28.0
plotly>=5.17.0

# --- LLM clients ------------------------------------------------------------
# Optional at runtime. Absent credentials or packages fall back to the
# deterministic offline narrator.
anthropic>=0.20.0
openai>=1.10.0

# --- Utilities --------------------------------------------------------------
tqdm>=4.65.0
python-dotenv>=1.0.0
